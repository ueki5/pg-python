[project]
name = "pg000"
version = "0.1.0"
description = "Python learning workspace"
readme = "README.md"
requires-python = ">=3.12"

[workspace]
members = [
    "pg001",
    "pg002",
]

[tool.uv.workspace]
members = [
    "pg001",
    "pg002",
]
